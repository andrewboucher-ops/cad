{
  "name": "cccs-poc",
  "version": "0.1.0",
  "private": true,
  "description": "Critical communications, dispatch and control-room proof of concept (simulation only).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": { "node": ">=18" }
}
